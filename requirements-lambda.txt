# Lambda Runtime Dependencies Only
# Core AWS and configuration
boto3>=1.34.72,<2.0.0
botocore>=1.34.72,<2.0.0
python-dotenv==1.0.0
pydantic==1.10.13  # Using v1 to avoid binary compatibility issues in Lambda

# API (minimal for Lambda)
fastapi==0.109.0
uvicorn==0.25.0
httpx==0.25.2
requests==2.31.0
mangum==0.17.0  # AWS Lambda adapter for FastAPI

# AWS Bedrock and AI (core only)
anthropic==0.8.1

# Compliance and security
cryptography>=41.0.0,<47.0.0
python-jose[cryptography]==3.3.0
PyJWT==2.8.0

# Data processing (essentials only)
pyyaml==6.0.1
jsonschema==4.20.0

# Code analysis (lightweight)
bandit==1.7.5

# Monitoring and logging
structlog==23.2.0
python-json-logger==2.0.7

# Utilities
tenacity==8.2.3
email-validator==2.1.0.post1
